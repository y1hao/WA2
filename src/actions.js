const query = require('./query')
const update = require('./update')
const acronym = require('./acronym')
const list = require('./list')
const random = require('./random')

module.exports = { query, update, acronym, list, random }